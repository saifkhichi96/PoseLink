<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.saifkhichi.poselink">

    <!-- Camera and Storage permissions -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.saifkhichi.poselink.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".app.SettingsActivity"
            android:exported="true"
            android:label="@string/title_settings"
            android:parentActivityName="com.saifkhichi.poselink.app.camera.CameraActivity" />
        <activity
            android:name="com.saifkhichi.poselink.app.sensors.ImuViewerActivity"
            android:exported="true"
            android:label="@string/title_imu"
            android:parentActivityName="com.saifkhichi.poselink.app.camera.CameraActivity" />
        <activity
            android:name=".app.RecordingsActivity"
            android:exported="true"
            android:label="@string/title_recordings"
            android:parentActivityName="com.saifkhichi.poselink.app.camera.CameraActivity" />
        <activity
            android:name=".app.AboutActivity"
            android:exported="true"
            android:label="@string/title_about"
            android:parentActivityName="com.saifkhichi.poselink.app.camera.CameraActivity" />
        <activity
            android:name=".app.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.saifkhichi.poselink.app.camera.CameraActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity" />
    </application>

</manifest>